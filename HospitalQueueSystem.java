

public class HospitalQueueSystem {
    public static void main(String[] args) throws Exception {
        // Window size is determined here since layout is not adjusted to be resizable
        int windowWidth = 750;
        int windowHeight = 500;

        new MainFrame(windowWidth, windowHeight);
    }
}
